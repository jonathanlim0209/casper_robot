twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic   : cmd_vel
        timeout : 0.5
        priority: 100

      tracker:
        topic   : cmd_vel_tracker
        timeout : 0.5
        priority: 70

      ball_tracker:
        topic   : cmd_vel_tracker
        timeout : 0.5
        priority: 70

      nav2:
        topic   : cmd_vel_nav
        timeout : 0.5
        priority: 60  
